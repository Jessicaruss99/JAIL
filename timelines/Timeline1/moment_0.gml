/*
with (instance_create_layer(x+32, y+32, "Instances", obj_texbox)) {
	depth = 100
}
*/

with (instance_create_layer(x+32, y+32, "Instances", obj_texboxPlayer)) {
	depth = 100
}

//Timer Starts for level
